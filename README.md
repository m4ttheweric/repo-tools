# matts-utils

Personal dev tools, scripts, and git hooks.

## Layout

- **scripts/** — standalone CLI tools (node, swift)
- **git-hooks/** — per-repo git hook overrides that layer on top of existing hooks (e.g. husky)
- **shell/** — shell config fragments meant to be sourced from `.zshrc`
